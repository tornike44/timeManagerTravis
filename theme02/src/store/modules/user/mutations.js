export default {
    setUsersList(state, data) {
        state.users = data;
    },
    setUser(state, data) {
        state.user = data;
    },
    delUser(state, data) {
        state.user = data;
    }
};
